{
  "name": "hypur-packages",
  "private": true,
  "scripts": {
    "build": "cd ./packages/cli && bun run build && cd ../../packages/runtime && bun run build && cd ../../packages/router && bun run build"
  },
  "workspaces": [
    "packages/*"
  ]
}
