{
  "name": "hypur-router",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ../../builds/hypur && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
