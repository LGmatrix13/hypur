{
  "name": "create-hypur",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ../../builds/hypur-router --minify && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module"
}
