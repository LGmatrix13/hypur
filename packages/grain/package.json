{
  "name": "@hypur/grain",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build:grain": "bun build ./src/index.ts --outdir ../../builds/grain --minify && bun run build:declaration",
    "build:router": "bun build ./src/router.ts --outdir ../../builds/grain --minify",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
